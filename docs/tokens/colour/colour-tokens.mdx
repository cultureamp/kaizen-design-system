import { Meta, Story } from "@storybook/blocks"
import * as ColourTokensStories from "./colour-tokens.stories"

<Meta of={ColourTokensStories} />

# Colour Tokens

## Usage

### Tailwind

Colours are accessible via normal tailwind prefixes ([example: text](https://tailwindcss.com/docs/text-color)) eg. `bg-*`, `text-*`, `border-*`

```html
<div class="bg-blue-500">
  ...
</div>
```

### SCSS
```scss
  @import "~@kaizen/design-tokens/sass/color"

  .mySCSSClassName {
    background: $color-blue-500;
  }
```

### CSS

Note: CSS Vars are only available if you install (Kaizen's global CSS file)[http://localhost:6006/?path=/docs/introduction-getting-started--docs#4-add-kaizens-global-css-stylesheet]

```css
  .myCSSClassName {
    background: vars(--color-blue-500);
  }
```

### JS

```javascript
import { tokens } from "@kaizen/design-tokens/js"

const myAmazingFunction = () => {
  const mainColor = data ? tokens.colors.purple[500]: tokens.colors.purple[300];

  return mainColor;
}
```

## Colours

<Story of={ColourTokensStories.ColorTokens} />
