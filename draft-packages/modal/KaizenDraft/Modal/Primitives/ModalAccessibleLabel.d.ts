import * as React from "react";
interface Props {
    readonly children: React.ReactNode;
}
declare type ModalAccessibleLabel = React.FunctionComponent<Props>;
declare const ModalAccessibleLabel: ModalAccessibleLabel;
export default ModalAccessibleLabel;
