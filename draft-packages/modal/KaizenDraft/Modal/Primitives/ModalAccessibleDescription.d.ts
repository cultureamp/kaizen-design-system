import * as React from "react";
interface Props {
    readonly children: React.ReactNode;
}
declare type ModalAccessibleDescription = React.FunctionComponent<Props>;
declare const ModalAccessibleDescription: ModalAccessibleDescription;
export default ModalAccessibleDescription;
