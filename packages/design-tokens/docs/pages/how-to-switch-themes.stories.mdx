import { Meta } from '@storybook/addon-docs'
import { LinkToStory, CodeBlock } from '../DocsComponents'
import gettingStartedPage from './getting-started.stories.mdx'

<Meta title="Design Tokens/How to switch themes" parameters={{ docsOnly: true }} />


# How to switch themes

## 1. Setup a ThemeManager

Ensure you have followed the steps in <LinkToStory storyModule={gettingStartedPage} /> to setup your application for using design tokens.

## 2. Use `setAndApplyTheme`

You can use the `setAndApplyTheme` method to change the current theme, to either Heart or a customised object.

Example:
```typescript
themeManager.setAndApplyTheme(heartTheme)

// or make your own modifications
themeManager.setAndApplyTheme({
  ...heartTheme,
  themeKey: "custom",
  spacing: {
    ...heartTheme.spacing,
    md: "1rem",
  },
})

```
