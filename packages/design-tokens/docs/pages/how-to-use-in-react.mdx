import { Meta } from "@storybook/blocks"
import { LinkTo } from "~storybook/components/LinkTo"

<Meta title="Systems/Tokens/How to use design tokens in React components" parameters={{ docsOnly: true }} />

# How to use design tokens in React components

## 1. Set up a KaizenProvider

Ensure you have followed the steps in <LinkTo pageId="components-kaizen-provider-installation">KaizenProvider</LinkTo> to set up your application for using themes.

## 2. Start using design tokens with the `useTheme` hook

Once the theme context is available, the `useTheme` hook will allow you to subscribe components and give access to the current theme, and re-render whenever it changes.

Example:

```jsx
import { useTheme } from "@kaizen/design-tokens"

const Component = () => {
  const theme = useTheme();

  return (
    <div style={{
      backgroundColor: theme.color.purple["500"] // #844587
    }}>
      Custom Kaizen theme component
    </div>
  )
}
```
