import { Meta } from "@storybook/blocks"
import { LinkTo } from "~storybook/components/LinkTo"

<Meta title="Systems/Tokens/Access tokens via JS" parameters={{ docsOnly: true }} />

# Access tokens via JSX

## 1. Set up a KaizenProvider

Ensure you have followed the steps in <LinkTo pageId="components-kaizen-provider-installation">KaizenProvider</LinkTo> to set up your application for using themes.

## 2. Use Tokens

### In JSX via CSS Vars

Because we use CSS vars, you can utilise them in your JSX as you would in CSS.

For example:

```jsx

const Component = () => (
  <div style={{
    backgroundColor: vars(--color-purple-500) // #844587
  }}>
    Custom Kaizen theme component
  </div>
)
```

### In JS functions

You can access the tokens directly:

```jsx
import { color } from "@kaizen/design-tokens/tokens/color.json"

const myAmazingFunction = () => {
  const mainColor = data ? colors.purple[500]: colors.purple[300];

  return mainColor;
}
```
