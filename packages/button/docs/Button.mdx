<!-- Button.mdx -->

import { Canvas, Meta, Story, Source, Props } from "@storybook/addon-docs"
import arrowRight from "@kaizen/component-library/icons/arrow-right.icon.svg"
import addIcon from "@kaizen/component-library/icons/add.icon.svg"
import trashIcon from "@kaizen/component-library/icons/trash.icon.svg"
import filterIcon from "@kaizen/component-library/icons/filter.icon.svg"
import { InlineNotification } from "@kaizen/notification";
import { LoadingInput } from "@kaizen/loading-skeleton"

import { Button, IconButton } from "@kaizen/button"

<Meta title="Design System/Button" component={Button} />

# Button

[Source code](https://github.com/cultureamp/kaizen-design-system/tree/master/packages/button)
&nbsp;|&nbsp;
[Component guidelines](https://cultureamp.design/components/button/)
&nbsp;|&nbsp;
[UI Kit](https://www.figma.com/file/eZKEE5kXbEMY3lx84oz8iN/%E2%9D%A4%EF%B8%8F-UI-Kit%3A-Heart?node-id=1929%3A17364)

| npm | [View on npm](https://www.npmjs.com/package/@kaizen/button)
| ---- | ---- 
| **Install** | `npm install @kaizen/button` |
| **Import** | `import { Button } from "@kaizen/button"`. This Button supersedes "@kaizen/draft-button" |
<br />

## Overview

Buttons perform actions. If it needs to navigate somewhere and can be opened in a new tab, use a link instead.

<Canvas mdxSource="<Button label='Label' />">
  <Button label="Label" />
</Canvas>

## Skeleton State

Use the `LoadingInput` component from the `LoadingSkeleton` package. Please refer to the LoadingInput [Component guidelines](https://cultureamp.design/components/loading-skeleton/).

<Canvas mdxSource="<LoadingInput isAnimated width={10} />">
  <LoadingInput isAnimated width={13} />
</Canvas>

## Component API
<Props />

<InlineNotification
  forceMultiline
  hideCloseIcon
  title="Need an icon only button?"
  type="informative"
>
  To display an icon only button, use our <a href="http://localhost:6006/?path=/docs/actions-icon-button--default-kaizen-demo-icon">
    IconButton
  </a> component.
</InlineNotification>

### Button Variants

Kaizen has five variants of button, `Default`, `Primary`, `Destructive`, `Secondary` and `Seconday` + `Destructive`.

If no `variant` is specified, a `Default` button will be rendered. 

For more information on when to use each variant, check out the [Component guidelines](https://cultureamp.design/components/button/)

<Canvas mdxSource="<Button label='Label' />
<Button label='Label' primary />
<Button label='Label' destructive />
<Button label='Label' secondary />
<Button label='Label' secondary destructive />">
  <Button label="Default" />
  <Button label="Primary" primary />
  <Button label="Destructive" destructive />
  <Button label="Secondary" secondary />
  <Button label="Secondary Destructive" secondary destructive />
</Canvas>

### Button `disabled`

A disabled Button prevents user interaction. It doesn’t appear in the tab order, can’t receive focus, and may not read aloud by a screenreader.

<Canvas mdxSource="<Button label='Label' disabled/>">
  <Button label="Label" disabled/>
</Canvas>

### Button `icon`

When a Button is supplied to the `icon` prop, it displays an icon.

```js
import trashIcon from "@kaizen/component-library/icons/trash.icon.svg"
```
Once the icons are imported, you can pass them directly into the Button component. 

### Button `iconPosition`

The consumer can specify the icon placement with the `iconPosition` prop, default position is `start`.

<Canvas mdxSource="<Button label='Label' icon={addIcon} />
<Button label='Label' icon={arrowRight} iconPosition='end' />">
  <Button label="Label" icon={addIcon} />
  <Button label="Label" icon={arrowRight} iconPosition="end" />
</Canvas>

### Button `badge`
You can display a `Badge` component within the button using the `badge` prop.

<Canvas mdxSource="<Button
    label='Label'
    icon={addIcon}
    icon={filterIcon}
    badge={{ text: '3', variant: 'active' }}
    secondary
  />">
  <Button
    label="Label"
    icon={addIcon}
    icon={filterIcon}
    badge={{ text: "3", variant: "active" }}
    secondary
  />
</Canvas>

### Button `fullWidth`

Buttons can be stretched to fill the full width of their container.

<Canvas mdxSource="<Button label='Label' fullWidth/>">
  <Button label="Label" fullWidth/>
</Canvas>


### Button `Working`

The `working` prop should be used in situations where a button action triggers a change in UI state but needs to wait for a server response, such as submitting a form

In conjuction use the `workingLabel` prop to update the label of the button when the working state is triggered. 

Alternatively use the `workingLabelHidden` prop to hide the button label all together. 

<Canvas mdxSource="<Button label='Label' working workingLabel='Submitting' />
<Button label='Label' working workingLabel='Submitting' workingLabelHidden />">
  <Button label="Label" working workingLabel="Submitting" />
  <Button label="Label" working workingLabel="Submitting" workingLabelHidden />
</Canvas>
