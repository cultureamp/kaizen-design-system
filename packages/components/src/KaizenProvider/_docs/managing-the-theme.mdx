import { Meta } from "@storybook/blocks"

<Meta title="Components/Kaizen Provider/Managing the theme" />

# Managing the theme

Out of the box, the `KaizenProvider` sets the `defaultTheme`. However if you want to roll your own theme, you can construct a Theme object and pass it in.

```jsx
export const customTheme = {
  themeKey: "custom",
  spacing: {
    md: "1rem",
  },
};

<KaizenProvider theme={customTheme}>
  <App />
</KaizenProvider>
```

## How to switch themes

You can use any method you want to switch themes, one way is React state.

Example:

```js
import { defaultTheme } from "@kaizen/components"
const [ useCustomTheme, setUseCustomTheme ] = useState(false)

useEffect(() => {
  // Conditional logic to change the boolean
  setUseCustomTheme(data ? true : false)
}, []);

<KaizenProvider theme={useCustomTheme ? customTheme : defaultTheme}>
  <App />
</KaizenProvider>
```

## Using the theme in apps

For more details on how to apply a theme, checkout our [design tokens documentation](/story/design-tokens-how-to-use-design-tokens-in-react-components--page).
