import { Canvas, Meta } from '@storybook/blocks'
import { ResourceLinks, KAIOInstallation } from '~storybook/components'
import * as PickerStories from './Picker.stories'
import { Picker } from '../index'

<Meta of={PickerStories} />

# Picker

## Overview

A Picker

Single Select

<Canvas of={PickerStories.SingleSelect} />

Multi Select

<Canvas of={PickerStories.MultiSelect} />

Search Single Select

<Canvas of={PickerStories.ComboSelect} />

Search Multi Select

<Canvas of={PickerStories.ComboMultiSelect} />

Nested Select

<Canvas of={PickerStories.NestedSelect} />
