import React from "react"
import {
  Popover as RACPopover,
  PopoverProps as RACPopoverProps,
} from "react-aria-components"

/**
 *
 */
export const Popover = (props: RACPopoverProps): JSX.Element => (
  <RACPopover {...props} />
)
