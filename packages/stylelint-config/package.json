{
  "name": "@kaizen/stylelint-config",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepublish": "tsc --project tsconfig.dist.json",
    "build": "yarn clean && yarn prepublish",
    "build:watch": "yarn clean && yarn prepublish --watch",
    "clean": "rimraf '**/*.js.map' 'index.js' 'index.js.map' 'index.d.ts' 'src/**/*.d.ts'"
  },
  "dependencies": {
    "postcss": "^8.2.10",
    "stylelint": "^13.6.0"
  }
}
