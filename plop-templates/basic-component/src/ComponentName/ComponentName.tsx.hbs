import React, { HTMLAttributes } from "react"
import classnames from "classnames"
import { OverrideClassName } from "@kaizen/component-base"
import styles from "./{{pascalCase componentName}}.scss"

/**
 * @todo: Refer to `docs/` for further code standards and guidelines
 */

/**
 * @todo: Replace `HTMLAttributes<HTMLElement>` with attributes/props you need to extend
 */
export interface {{pascalCase componentName}}Props extends OverrideClassName<HTMLAttributes<HTMLDivElement>> {
  /** @todo: Add custom props here */
}

export const {{pascalCase componentName}}: React.VFC<{{pascalCase componentName}}Props> = ({
  classNameOverride,
  ...restProps
}) => {
  return (
    <div
      className={classnames([
        styles.{{camelCase componentName}},
        classNameOverride,
      ])}
      {...restProps}
    />
  )
}

{{pascalCase componentName}}.displayName = "{{pascalCase componentName}}"
