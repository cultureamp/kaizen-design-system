import { Canvas, Meta, Controls, ArgTypes, DocsStory } from '@storybook/blocks'
import { ResourceLinks, KAIOInstallation, LinkTo, DocHeading } from '~storybook/components'
import * as {{pascalCase componentName}}Stories from './{{pascalCase componentName}}.stories'

<Meta title="Components/{{pascalCase componentName}}/{{pascalCase componentName}} {{#if isNext}}(next){{/if}}/API Specification" tags={['{{#if isNext}}next{{else}}current{{/if}}', 'dev docs']} />
<DocHeading title="{{pascalCase componentName}}" componentVersion="{{#if isNext}}next{{else}}current{{/if}}" docTags={['dev docs']} />

{ /* @todo: Adjust Github link (if needed), add Figma and Design Guidelines links */}

<ResourceLinks
  sourceCode="https://github.com/cultureamp/kaizen-design-system/tree/main/packages/components/src/{{pascalCase componentName}}"
  figma=""
  designGuidelines=""
/>

<KAIOInstallation exportNames="{{pascalCase componentName}}" {{#if isNext}}isNext{{/if}} />

## Overview

{/* @todo: Update summary. */}
Brief summary of the component here.

<Canvas of={{ preCurly pascalCase componentName}}Stories.Playground} />

## Props

{/* @todo: Use `include` or `exclude` to ensure the controls represent the most essential controls */}

<Controls
  of={{preCurly (pascalCase componentName)}}Stories.Playground} 
  include={[
    {
      /* @todo: if this component exposes a lot of props you might want to add a consolidated list with essential props to showcase the core functionality. Any additional one can go in the "Additional options" section */
    }
  ]}
/>


{/* @todo: If there are any important notes to call out here regarding use of this component, this should go here. ie: "Buttons vs links" or "Navigation and native anchor attributes" */}

## Component contextual information title

Lorem ipsum dolor sit amet consectetur adipisicing elit...

## Examples

{/* @todo: document the various props and patterns for this components using the Storybook `Canvas` element */}

### Prop 1

<Canvas of={{preCurly pascalCase componentName}}Stories.Prop1Example} />

### Prop 2

<Canvas of={{preCurly pascalCase componentName}}Stories.Prop1Example} />

## Additional options

{/* @todo: Only include this section if we are exposing a large number of props, of which many are uneccesary, ie: React Aria. */}

<ArgTypes
  of={{preCurly pascalCase componentName}}Stories.Playground}
  exclude={[
    {
      /* @todo: exclude anything that has been called out in the Props table */
    }
  ]}
/>
