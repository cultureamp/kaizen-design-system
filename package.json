{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "prettier --write '**/*'",
    "lint": "tslint -p . && prettier --check '**/*'",
    "reset": "scripts/reset"
  },
  "devDependencies": {
    "lerna": "^3.15.0",
    "prettier": "^1.18.2"
  }
}
