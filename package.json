{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "tsc --noEmit",
    "format": "prettier --write '**/*'",
    "lint": "tslint -p . && prettier --check '**/*'",
    "reset": "scripts/reset"
  },
  "devDependencies": {
    "lerna": "^3.15.0",
    "prettier": "^1.18.2",
    "typescript": "^3.5.1"
  }
}
