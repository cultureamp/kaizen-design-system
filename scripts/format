#!/bin/bash
set -eo pipefail

helpers="$(dirname $0)/helpers"
source "${helpers}/newline"
source "${helpers}/bin"

newline "Running auto-formatters..."
$bin/tslint -p . --fix
$bin/prettier --write "**/*"
